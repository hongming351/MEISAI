# DWTS数据分析项目依赖包
# 注意：NumPy版本需要限制在2.0以下以确保与其他包的兼容性

# 核心数据处理和分析
pandas>=1.5.0,<2.1.0
numpy>=1.21.0,<2.0.0  # 限制NumPy在1.x版本，避免2.0的兼容性问题

# 可视化
matplotlib>=3.5.0,<3.9.0  # 限制版本以确保与NumPy 1.x兼容
seaborn>=0.11.0,<0.13.0

# 科学计算和优化
scipy>=1.7.0,<1.12.0
scikit-learn>=1.0.0,<1.4.0

# 机器学习相关
xgboost>=1.7.0,<2.1.0  # 添加xgboost，这是你需要的
shap>=0.41.0,<0.45.0   # 添加SHAP，限制版本以确保兼容性

# 加速计算（可选）
numba>=0.56.0,<0.59.0  # Numba需要与NumPy 1.x兼容

# Jupyter Notebook支持（可选）
jupyter>=1.0.0
ipykernel>=6.0.0

# 数据探索和分析
plotly>=5.0.0,<5.19.0
bokeh>=2.4.0,<3.3.0

# 统计分析
statsmodels>=0.13.0,<0.14.0

# 日期时间处理
python-dateutil>=2.8.0

# 系统工具
tqdm>=4.60.0,<4.66.0

# Excel文件支持
openpyxl>=3.0.0,<3.1.0  # Excel文件支持
xlrd>=2.0.0,<2.1.0      # 旧版Excel文件支持

# 数据验证（可选）
pandera>=0.11.0,<0.19.0  # 数据验证库

# 交互式可视化（可选）
ipywidgets>=7.6.0,<8.2.0

# 报告生成（可选）
nbconvert>=6.0.0,<7.0.0